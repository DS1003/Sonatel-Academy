//saisir un nombre parfait puis l'utilisateur doit saisir le suivant s'il ne le fait pas après 10 tentatives  
//il est déclaré perdu. S'il trouve deux fois de suite on lui demande s'il veut continuer ou pas ?
#include <stdio.h>
#include <stdlib.h>

int nextperfect (int);
int saisie (void);
int isperfect (int);


//------------------------------------------- main ------------------------------------------------------------
int main() {

    return 0;
}
//---------------------------------- Fonctions & Procédures ---------------------------------------------------

int isperfect (int n) {

}